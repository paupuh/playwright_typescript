{
  "default": {
    "formatOptions": {
      "snippetInterface": "async-await"
    },
    "paths": ["src/test/features/"],
"require": ["src/test/steps/*.ts", "src/test/pom/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["progress", "json:reports/cucumber_report.json"]
  }
}
